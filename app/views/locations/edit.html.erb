<h2>Edit City Form</h2>

<%= render 'form' %>

<%= link_to "Go Back", location_path(@location) %>
  <% if current_user.admin?  %>
    <%= button_to 'Delete City', @location, method: :delete, data: { confirm: 'Are you sure?'} %>
  <% end %>
